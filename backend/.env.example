# Server Configuration
NODE_ENV=production
BACKEND_PORT=5000
CORS_ORIGIN=https://do-sensor-dashboard.vercel.app

# Database Configuration (PostgreSQL)
# Render will automatically provide this - format: postgresql://user:password@host:5432/dbname
DATABASE_URL=postgresql://user:password@localhost:5432/do_sensor_db

# JWT Configuration
# Generate a secure random string for production (at least 32 characters)
JWT_SECRET=change-this-to-a-secure-random-string-in-production
JWT_EXPIRY=24h

# Device Configuration
# Set your Pi device ID that will send sensor data
DEVICE_ID=sensor-001
DEVICE_API_KEY=your-device-api-key-from-pi

# Backend URL for frontend config
BACKEND_URL=https://do-sensor-backend.onrender.com
